### Changed

- Updated frontend dependencies for React, React DOM, Lucide, Vite, and TypeScript ESLint so the app ships with the latest validated dependency set.
- Refreshed the Tauri dependency lockfile within the compatible release range to keep the Windows build and updater pipeline current.

### Fixed

- Cleared the open GitHub Security and quality items after the previous release by resolving CodeQL findings and documenting the remaining upstream-only advisory risk.
- Closed the pending Dependabot pull request queue by applying the validated updates directly to the release branch.
- Removed unused command-line dependency paths from the noise suppression stack, reducing unnecessary transitive package exposure.
