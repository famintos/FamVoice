### Fixed

- Fixed update checks that could fail silently and still show "No update available" in Settings.
- Fixed failed update installs hiding the available update instead of letting you retry from the same screen.

### Changed

- Changed the Settings update panel to show the real updater error when FamVoice cannot check for a new version.
- Changed the release workflow to verify that the published updater feed matches the shipped version and includes the required Windows update targets.
