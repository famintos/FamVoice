### Added

- Added a clear red warning ring around the widget when your microphone is not picking up usable voice input, so you do not keep dictating into silence.

### Changed

- Changed the widget warning treatment so missing API configuration now uses the same visible warning ring for a more consistent at-a-glance state.

### Fixed

- Fixed Windows mouse hotkey capture by switching to a lower-level mouse hook path that avoids the previous listener issues.
