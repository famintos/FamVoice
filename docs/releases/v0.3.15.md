### Changed

- Changed the app header to use a subtler branded entrance when the main window opens.
- Changed the idle widget and recording surfaces to keep the recent Faminto visual refresh without adding extra motion in the widget itself.

### Fixed

- Fixed release builds so FamVoice no longer depends on a sibling `FamBrand` checkout being present on the build machine.
- Fixed CI and GitHub release packaging by vendoring the required brand tokens directly inside the app repository.
