### Fixed

- Fixed the Settings update check being blocked by Tauri ACL permissions in the shipped app.
- Fixed manual update refresh in Settings failing with `plugin:updater|check not allowed by ACL`.

### Changed

- Changed the Settings window capability to allow updater checks and app relaunch for the manual update flow.
