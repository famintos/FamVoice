### Fixed

- Fixed prompt improvement results that could still come back sounding like raw dictation instead of an execution-ready coding prompt.
- Fixed prompt optimization fallback behavior so low-quality rewrites are rejected instead of being treated as final output.

### Changed

- Changed prompt optimization to use the saved OpenAI API key instead of a separate Anthropic key.
- Changed the Prompt Optimization settings flow to use GPT-5.4 Mini and GPT-5.4 Nano model options.

### Added

- Added stronger prompt optimizer validation to catch conversational or malformed rewrites before they reach the final output.
