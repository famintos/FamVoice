### Changed

- Changed the main desktop window to a tighter 260x175 layout with a simpler centered record view and lighter update banner.
- Changed the settings screen with local dropdown controls, cleaner inline update states, and a refreshed visual treatment.
- Changed the compact widget to use the new Faminto mark and a subtler idle animation.

### Fixed

- Fixed bundled fonts so the refreshed interface uses local assets instead of blocked remote Google Fonts in signed Tauri releases.
- Fixed version metadata drift between the frontend package, Tauri config, and Rust crate so release artifacts publish the same app version.
- Fixed frontend regression coverage to match the current UI structure and release flow.
