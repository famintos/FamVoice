### Changed

- Glossary replacements now match spoken terms more flexibly, so entries like `FEM Design` can also catch joined or differently cased dictation such as `FemDesign` and `FEMDESIGN`.
- Updated the Settings glossary copy to explain that replacements now handle simple spacing and casing variations.

### Fixed

- Prevented glossary phrase replacements from firing inside larger words, which keeps terms like `SuperFemDesignTool` untouched.
- Kept space-flexible glossary matching scoped to whitespace only, so punctuation-separated terms such as `Fem-Design` are not rewritten unexpectedly.
