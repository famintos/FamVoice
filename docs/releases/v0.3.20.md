### Changed

- Updated FamVoice's bundled desktop runtime and build dependencies to current patch versions so the app stays easier to maintain and package reliably.
- Refreshed the updater stack used by the desktop app, with no settings or workflow changes required on your side.

### Fixed

- Improved compatibility between the built-in updater components used by FamVoice, which helps keep future app updates more predictable.
- Reduced the risk of issues caused by older runtime dependencies while keeping the current interface and behavior intact.
